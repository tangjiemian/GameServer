package com.server.commonutils;

public class GameSkillUtils {
	
	
	/*
	 * public static HeroSkillBean combineSkill(HeroSkillBean heroSkillBean) {
	 * HeroSkillBean createBean = new HeroSkillBean(); KernelSkillConfig config =
	 * GameSkillKernelProvider.getSkill(heroSkillBean.skillid); createBean.value =
	 * heroSkillBean.value + config.getValue(); return createBean; }
	 */

}
